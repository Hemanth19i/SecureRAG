# Dev: the browser calls the same-origin /api prefix; Vite proxies it to the
# Flask backend (see vite.config.ts -> server.proxy). Override the proxy target
# with VITE_API_PROXY_TARGET if the backend runs elsewhere.
VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://localhost:5000
