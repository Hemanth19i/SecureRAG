[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: end-to-end test using the real embedder + Chroma (run with -m integration)
addopts = -m "not integration" --cov=intelligence --cov=api --cov-report=term-missing
